/* ==========================================================================
   js/state.js — 🔄 여러 파일이 함께 쓰는 "현재 상태" 변수
   --------------------------------------------------------------------------
   프로그램이 실행되는 동안 값이 계속 바뀌고, 여러 JS 파일에서 읽고 씁니다.
   "지금 누가 로그인했지?", "지금 어떤 학생의 어떤 시험 체크리스트가 열려 있지?" 같은 정보입니다.
   (한 파일 안에서만 쓰는 상태는 그 파일 안에 따로 있습니다)

   ※ 이 프로젝트의 JS 파일들은 import/export 모듈이 아니라 일반 <script> 로
     불러오기 때문에, 한 파일에서 만든 변수·함수를 다른 파일에서 그대로 쓸 수 있습니다.
     대신 서로 다른 파일에서 같은 이름을 또 만들면(let/const) 오류가 나니 주의하세요.
   ========================================================================== */

// 로그인한 선생님 이름 (로그인 전이면 null)
// 바뀌는 곳: login.js
let currentTeacher = null;

// 지금 체크리스트가 열린 학생 { id, name, school, grade, className, summary } (없으면 null)
// 바뀌는 곳: student-list.js
let currentStudent = null;

// 명단에 없는 이름으로 로그인해서 "빈 체크리스트"를 쓰는 중인지 (true 면 서버에 저장하지 않음)
// 바뀌는 곳: student-list.js 의 openBlankChecklist / closeChecklist / openChecklist
let isBlankChecklist = false;

// 왼쪽에서 고른 시험 (예: "1학기 기말고사"). 모든 학생의 체크리스트가 이 시험 기준으로 열립니다.
// 기본값은 공란("") — 시험을 고르기 전에는 학생 체크리스트를 열지 않습니다 (빈 체크리스트는 사용 가능).
// 바뀌는 곳: student-list.js
let currentExam = "";

// 지금 열린 체크리스트의 학년 코드 (= currentStudent.grade). 표·단원 설정을 그릴 때 씁니다.
// 바뀌는 곳: student-list.js
let currentGrade = "mid1";

// 페이지를 처음 열었을 때 종합 의견 칸의 내용 (main.js 에서 채움)
// 새로 작성하는 체크리스트는 의견 칸이 이 값으로 시작합니다.
let DEFAULT_OPINION = "";
